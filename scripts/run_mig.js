const { createClient } = require('@supabase/supabase-js'); const s = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); const sql = ALTER TABLE public.companies ADD COLUMN IF NOT EXISTS office_lat numeric(10, 8), ADD COLUMN IF NOT EXISTS office_lng numeric(11, 8), ADD COLUMN IF NOT EXISTS office_radius_meters integer DEFAULT 100; ALTER TABLE public.attendances ADD COLUMN IF NOT EXISTS is_manual boolean DEFAULT false, ADD COLUMN IF NOT EXISTS manual_reason text, ADD COLUMN IF NOT EXISTS approval_status text DEFAULT 'approved';; s.rpc('exec_sql', { sql }).then(console.log);  
